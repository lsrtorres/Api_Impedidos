# ============================================================
# Copie este arquivo para .env e preencha com os valores reais
# NUNCA commite o arquivo .env no repositório
# ============================================================

# --- Segurança JWT ---
# Gere com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=troque_por_valor_aleatorio_longo

# --- Certificado digital SERPRO (e-CNPJ) ---
PFX_PATH=/home/azureuser/projetos/e-CNPJ_F12.p12
SENHA_PFX=sua_senha_pfx
# Cache persistente do token GOV (sobrevive a restarts)
TOKEN_FILE=/home/azureuser/projetos/impedidos/Api_Impedidos/certificates/token_gov.json

# --- Banco de dados SQL Server ---
DB_SERVER=187.87.134.107
DB_NAME=Api_Impedidos
DB_USER=leandro.torres
DB_PASS=sua_senha_db
